%h1 Cities
- if can? :create, City
  = link_to "Add city", new_city_path, class: "btn"

%table
  %thead
    %tr
      %th Name
      %th Coordinates
      %th
  %tbody
    - @cities.each do |city|
      %tr
        %td= city.name
        %td= "#{city.lat}, #{city.lon}"
        %td= link_to "Current", current_city_weather_path(city)
        %td= link_to "7 days weather", daily_city_weather_path(city)
